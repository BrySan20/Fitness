name: CI/CD

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Convert repository name to lowercase
        id: repo_name
        run: echo "repo_lowercase=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ steps.repo_name.outputs.repo_lowercase }}:latest
            ghcr.io/${{ steps.repo_name.outputs.repo_lowercase }}:${{ github.sha }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # 1. Navega a la carpeta del proyecto
            cd /home/deployer/proyecto
            
            # 2. Crea el archivo JSON secreto desde el Secret de GitHub
            echo '${{ secrets.FIREBASE_ADMIN_SDK_JSON }}' > fitness-tracker-pwa-firebase-adminsdk-fbsvc-ab44bd97d5.json
            
            # 3. Crea el archivo .env con las llaves VAPID
            cat << 'EOF' > .env
            VAPID_PUBLIC_KEY=${{ secrets.VAPID_PUBLIC_KEY }}
            VAPID_PRIVATE_KEY=${{ secrets.VAPID_PRIVATE_KEY }}
            EOF
            
            # 4. LIMPIEZA COMPLETA - Detén todo lo relacionado con docker-compose
            docker-compose down --remove-orphans || true
            
            # 5. Elimina el contenedor específico si existe
            docker container stop fitness_app || true
            docker container rm -f fitness_app || true
            
            # 6. Limpia imágenes viejas y volúmenes huérfanos
            docker image prune -af || true
            docker volume prune -f || true
            
            # 7. Crea el docker-compose.yml automáticamente
            cat << 'EOF' > docker-compose.yml
            version: '3.8'
            services:
              mi-app:
                image: ghcr.io/brysan20/fitness:latest
                container_name: fitness_app
                ports:
                  - "80:5000"
                restart: unless-stopped
                volumes:
                  - ./fitness-tracker-pwa-firebase-adminsdk-fbsvc-ab44bd97d5.json:/app/server/fitness-tracker-pwa-firebase-adminsdk-fbsvc-ab44bd97d5.json
                  - ./.env:/app/.env
            EOF
            
            # 8. Jala la nueva imagen de Docker (forzando descarga sin caché)
            docker-compose pull
            
            # 9. Inicia el nuevo contenedor limpiamente
            docker-compose up -d --remove-orphans
            
            # 10. Verifica que el contenedor esté corriendo
            docker ps | grep fitness_app